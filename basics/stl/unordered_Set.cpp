#include<bits/stdc++.h>
using namespace std;

int main(){
    unordered_set<int> ust;
    //same as set from java
    //all functions same as set, no sorting rest is same 
    //tc 0(1) in all
    //you can't uper bound and lower bound here   
    return 0;
}